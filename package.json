{
  "name": "employee-task-management",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start-app": "npm run dev --prefix app",
    "install-app": "npm i --prefix app",
    "start-services": "npm run start --prefix services",
    "install-services": "npm i --prefix services",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.7.2",
    "passport": "^0.7.0",
    "passport-jwt": "^4.0.1",
    "twilio": "^5.0.4"
  },
  "devDependencies": {
    "@types/passport": "^1.0.16",
    "@types/uuid": "^9.0.8"
  }
}
